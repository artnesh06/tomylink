"use client";
// User dashboard - settings, links, earnings

export default function DashboardPage() {
  return (
    <div style={{ background: "#000", minHeight: "100vh", color: "#fff", padding: 20 }}>
      <h1>Dashboard</h1>
      <p>Manage your links, profile, and earnings here.</p>
    </div>
  );
}
